<h1>Genre Form</h1>
<%= form_for @genre do |f| %>
<label>Genress Name: </label>
<%= f.text_field :name %>

<%= collection_select(:song, :song_id, Song.all, :id, :name) %>

<%= collection_select(:artist, :artist_id, Artist.all, :id, :name) %>

<%= f.submit %>
<% end %>
