<h1><%=@artist.name%></h1>
<p><%= @artist.bio %></p>
<ul>
<% @songs.each do |song| %>
    <li><%= link_to song.name, song_path(song) %></li>
<%end%>
</ul>
<%= button_to "Delete", artist_path(@artist), method: "delete" %>
